import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Colors.black;
  static const secondryColor = Colors.white;
  static const buttonColor = Colors.black;
  static const textColors = Colors.black;
  static const buttonTextColor = Colors.white;
  static const selectedItemColor = Colors.black;

  static const unselectedItemColor = Color.fromARGB(211, 158, 158, 158);
}
